# Backend dependencies. Install into the `dental-clinic` conda env only.
#   conda run -n dental-clinic pip install -r backend/requirements.txt

# API
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
# Multipart form parsing — required for FastAPI file uploads (UploadFile / Form).
# Added in 5.6 for patient file uploads (X-rays, photos, documents).
python-multipart==0.0.32

# Database — not used until step 0.5, pinned now so the env is stable.
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4

# Auth — REMOVED in 10.1. The app has no login: it is a single-user desktop app
# behind Windows' own sign-in, so there are no tokens to verify. pyjwt and
# cryptography went with the Supabase dependency. See app/auth.py.

# Tests
pytest==9.1.1
httpx==0.28.1
