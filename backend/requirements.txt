# Backend dependencies. Install into the `dental-clinic` conda env only.
#   conda run -n dental-clinic pip install -r backend/requirements.txt

# API
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# Database — not used until step 0.5, pinned now so the env is stable.
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4

# Auth — verify Supabase's ES256 access tokens against their JWKS endpoint.
# PyJWT's PyJWKClient fetches + caches the public keys; cryptography does the
# elliptic-curve signature check (ES256).
pyjwt[crypto]==2.13.0
cryptography==49.0.0

# Tests
pytest==9.1.1
httpx==0.28.1
