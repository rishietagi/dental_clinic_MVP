"""Business-logic services. Plain functions that operate on a DB session.

Kept separate from routers (HTTP concerns) and models (persistence) — this is
where Phase 2's patient/appointment/billing logic will live.
"""
