{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell only displays the local app; it exposes no Tauri APIs to the page.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
